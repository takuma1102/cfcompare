% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-panel.R
\name{panel_matrices}
\alias{panel_matrices}
\title{Reshape a long panel into TROP matrices}
\usage{
panel_matrices(data, outcome, treatment, unit, time)
}
\arguments{
\item{data}{A long \code{data.frame}/\code{tibble} with one row per unit-time cell.}

\item{outcome, treatment, unit, time}{Column names (strings). \code{treatment} must be
a 0/1 indicator.}
}
\value{
A list with components:
\describe{
\item{\code{Y}}{\verb{N x T} outcome matrix (rows = units, columns = times).}
\item{\code{W}}{\verb{N x T} 0/1 treatment matrix on the same grid.}
\item{\code{units},\code{times}}{the sorted unit and time labels.}
\item{\code{treated_units}}{integer row indices of units with any treated cell.}
\item{\code{treated_periods}}{number of periods in which any treated unit is
active, i.e. the width of the post block.}
}
}
\description{
Converts a long panel \code{data.frame} into the \verb{N x T} outcome (\code{Y}) and
treatment (\code{W}) matrices used by \code{\link[=trop_matrix]{trop_matrix()}}, and reports the treated rows
and the width of the treated block. It is the data-frame-to-matrix companion
to \code{\link[=trop_matrix]{trop_matrix()}}: rather than building \code{Y}, \code{W}, \code{treated_units} and
\code{treated_periods} by hand, call \code{panel_matrices()} and pass the pieces
straight through.
}
\examples{
df <- sim_panel(N = 14, T = 9, n_treated = 3, t0 = 6, att = 2, seed = 1)
pm <- panel_matrices(df, "y", "w", "id", "t")
str(pm)
# Feed straight into the matrix-in entry point (treated rows/periods inferred).
trop_matrix(pm$Y, pm$W, lambda_unit = 0.1, lambda_time = 0.1, lambda_nn = Inf)
}
\seealso{
\code{\link[=trop_matrix]{trop_matrix()}}, \code{\link[=trop]{trop()}}
}
