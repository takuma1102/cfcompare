% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{selected_lambda}
\alias{selected_lambda}
\title{CV-selected penalties from a TROP penalty-sensitivity grid}
\usage{
selected_lambda(grid)
}
\arguments{
\item{grid}{A \code{cf_trop_grid} from \code{\link[=trop_sensitivity]{trop_sensitivity()}}.}
}
\value{
A named list with the selected \code{time}, \code{unit} and \code{nn} penalties. The
full selected row (including the ATT and CV loss there) remains available in
\code{attr(grid, "selected")}.
}
\description{
Returns the cross-validation-selected penalties of a \code{\link[=trop_sensitivity]{trop_sensitivity()}}
grid --- the grid cell minimising the CV loss --- as a \code{list(time=, unit=, nn=)}, the same shape \code{\link[=trop]{trop()}} accepts for its \code{lambda} argument. So the
data-driven choice on a grid can be fed straight back into a fit, e.g.
\code{trop(..., lambda = selected_lambda(g))}, without digging the values out of
\code{attr(g, "selected")} by hand.
}
\examples{
\donttest{
df <- sim_panel(N = 20, T = 12, n_treated = 4, t0 = 9, att = 2, seed = 1)
g <- trop_sensitivity(df, "y", "w", "id", "t",
                      lambda_time = c(0, 0.25, 1),
                      control = trop_control(n_cv_cells = 10L, cv_cycles = 1L))
selected_lambda(g)
}
}
\seealso{
\code{\link[=trop_sensitivity]{trop_sensitivity()}}, \code{\link[=surface_matrix]{surface_matrix()}}, \code{\link[=trop]{trop()}}
}
