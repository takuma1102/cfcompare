% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{bind_att}
\alias{bind_att}
\title{Stack ATT results into one comparison table}
\usage{
bind_att(...)
}
\arguments{
\item{...}{Objects coercible by \code{\link[=as_att]{as_att()}}, optionally named to set the
\code{method} label.}
}
\value{
A \code{cf_att_tbl} (a \code{data.frame}) with the inputs stacked row-wise.
}
\description{
Row-binds several ATT results --- \code{\link[=trop]{trop()}} fits, \code{\link[=panel_compare]{panel_compare()}} results,
\pkg{synthdid} estimates, existing \code{cf_att_tbl}s, or anything \code{\link[=as_att]{as_att()}}
understands --- into a single \code{cf_att_tbl} ready for \code{\link[=autoplot]{autoplot()}} or
\code{\link[=plot_counterfactual]{plot_counterfactual()}}. Each argument is passed through \code{\link[=as_att]{as_att()}} first, so
multi-row inputs (e.g. a \code{panel_compare()} result) keep all their rows, and
columns are aligned automatically. This replaces the manual
\code{rbind(as.data.frame(...), ...)} + \code{class(...) <- "cf_att_tbl"} dance.
}
\details{
Names given to the arguments become \code{method} labels, overriding the label
already on a \emph{single-row} result; a name on a multi-row input is ignored with
a warning (it cannot sensibly relabel several methods at once). For per-object
control of other fields such as \code{outcome}, coerce that object with \code{\link[=as_att]{as_att()}}
first and pass the result in unnamed.
}
\examples{
df <- sim_panel(N = 14, T = 9, n_treated = 3, t0 = 6, att = 2, seed = 1)
lam <- list(time = 0.1, unit = 0.5, nn = 2)   # fixed penalties (fast example)
f_pooled   <- trop(df, "y", "w", "id", "t", anchor = "pooled",
                   lambda = lam, se = "none")
f_per_cell <- trop(df, "y", "w", "id", "t", anchor = "per_cell",
                   lambda = lam, se = "none")
bind_att(pooled = f_pooled, per_cell = f_per_cell)
}
\seealso{
\code{\link[=as_att]{as_att()}}, \code{\link[=panel_compare]{panel_compare()}}, \code{\link[=compare_se_modes]{compare_se_modes()}}
}
