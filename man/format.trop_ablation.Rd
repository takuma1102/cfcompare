% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ablation.R
\name{format.trop_ablation}
\alias{format.trop_ablation}
\title{Render a TROP ablation table as paste-ready LaTeX or Markdown}
\usage{
\method{format}{trop_ablation}(
  x,
  output = c("latex", "markdown"),
  digits = 3,
  caption = "TROP penalty ablation",
  label = "tab:trop-ablation",
  ...
)
}
\arguments{
\item{x}{A \code{trop_ablation} object from \code{trop_ablation()}.}

\item{output}{\code{"latex"} (booktabs) or \code{"markdown"} (GitHub pipe table).}

\item{digits}{Number of decimal places.}

\item{caption, label}{Table caption and cross-reference label (LaTeX only).}

\item{...}{Unused.}
}
\value{
A character vector of lines (paste-ready). Combine with
\code{cat(..., sep = "\\n")} or \code{writeLines()}.
}
\description{
Produces a publication-quality table from a \code{trop_ablation()} result. The
\code{"latex"} output uses \pkg{booktabs} rules and math-mode penalty headers,
matching the style of the paper's tables (load the booktabs LaTeX package).
}
\examples{
df <- sim_panel(N = 14, T = 8, n_treated = 3, t0 = 5, att = 2, seed = 1)
ab <- trop_ablation(df, "y", "w", "id", "t",
                    lambda_full = list(time = 0.1, unit = 0.5, nn = 2))
writeLines(format(ab, "markdown"))
}
\seealso{
\code{trop_ablation()}
}
