% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placebo_test.R
\name{trop_placebo_test}
\alias{trop_placebo_test}
\alias{print.trop_placebo_test}
\title{Placebo (randomisation) test for a TROP ATT}
\usage{
trop_placebo_test(
  object,
  B = 500L,
  alternative = c("two.sided", "greater", "less"),
  control = NULL
)

\method{print}{trop_placebo_test}(x, digits = 4, ...)
}
\arguments{
\item{object}{A \code{trop} fit from \code{\link[=trop]{trop()}}.}

\item{B}{Number of placebo assignments to draw.}

\item{alternative}{Direction of the test: \code{"two.sided"} (default), \code{"greater"}
or \code{"less"}.}

\item{control}{A \code{\link[=trop_control]{trop_control()}} list for the placebo refits (penalties are
held fixed at the fit's values; this mainly sets \code{seed} and \code{workers}).
Defaults to \code{\link[=trop_control]{trop_control()}}; set \code{seed} here for reproducible draws.}

\item{x}{A \code{trop_placebo_test} object.}

\item{digits}{Number of significant digits to print.}

\item{...}{Unused.}
}
\value{
An object of class \code{trop_placebo_test} (a list) with the \code{observed}
ATT, the vector of \code{placebo} ATTs, the \code{p.value}, the placebo-null \code{mean}
and \code{sd}, a central null interval \code{null.low}/\code{null.high}, \code{alternative},
\code{B}, and \code{n_treated_units}. It has \code{print()} and \code{\link[=autoplot]{autoplot()}} methods.
}
\description{
Assesses whether a fitted \code{\link[=trop]{trop()}} ATT is distinguishable from zero by
comparing it to a placebo null distribution. Control units are repeatedly
relabelled as pseudo-treated --- using the \emph{same number of treated units and
the same treatment timing} as the real design, so block, staggered and
non-absorbing patterns are all reproduced faithfully --- and the estimator is
refit at the fit's cross-validated penalties (the true effect is zero by
construction). The observed ATT is then placed against that null.
}
\details{
This is a randomisation/permutation test, not a standard error: it answers
"could a TROP design like mine produce an effect this large when there is none?"
For interval estimates use the bootstrap or jackknife (see \code{\link[=trop]{trop()}} and
\code{\link[=compare_se_modes]{compare_se_modes()}}).
}
\examples{
\donttest{
df  <- sim_panel(N = 40, T = 12, n_treated = 5, t0 = 9, att = 3, seed = 1)
fit <- trop(df, "y", "w", "id", "t", se = "none",
            control = trop_control(n_cv_cells = 10L, cv_cycles = 1L))
pt  <- trop_placebo_test(fit, B = 200)
pt
}
}
\seealso{
\code{\link[=trop]{trop()}}, \code{\link[=compare_se_modes]{compare_se_modes()}}
}
