% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trop_matrix.R
\name{trop_matrix}
\alias{trop_matrix}
\title{TROP estimate on matrix input}
\usage{
trop_matrix(
  Y,
  W,
  treated_units = NULL,
  lambda_unit,
  lambda_time,
  lambda_nn,
  treated_periods = NULL,
  control = trop_control()
)
}
\arguments{
\item{Y}{N x T outcome matrix.}

\item{W}{N x T 0/1 treatment matrix (1 = actively treated cell).}

\item{treated_units}{Integer row indices of the treated units, used to anchor
the unit weights. Defaults to \code{NULL}, in which case the treated rows are
inferred from \code{W} as the rows with at least one treated cell.}

\item{lambda_unit, lambda_time}{Non-negative decay parameters for the unit and
time weights.}

\item{lambda_nn}{Nuclear-norm penalty; use \code{Inf} to drop the low-rank term.}

\item{treated_periods}{Number of final columns treated as the post block, used
to build the pre-period mask and the time-distance centre. Defaults to
\code{NULL}, in which case it is inferred from \code{W} as the number of periods in
which any treated unit is active.}

\item{control}{A list of solver controls from \code{\link[=trop_control]{trop_control()}}.}
}
\value{
A single numeric ATT estimate.
}
\description{
A thin, matrix-in wrapper around the TROP working model, written independently
from the paper. The matrix-in form is convenient for numerically comparing
\code{\link[=trop]{trop()}} against other matrix-based implementations on identical inputs. For
data-frame input, cross-validated penalty selection, inference and the
multi-estimator comparison, use \code{\link[=trop]{trop()}} and \code{\link[=panel_compare]{panel_compare()}} instead.
}
\details{
The untreated outcome model is fitted on the control cells with the supplied
weights, and the returned effect is the average over treated cells of
\code{Y - alpha - beta - L}, exactly as in the paper's eq. (2). With
\code{lambda_nn = Inf} the low-rank term is dropped and the fit is weighted two-way
fixed effects, matching the reference to numerical tolerance. With a finite
\code{lambda_nn} the nuclear-norm term is solved by this package's
proximal-gradient / soft-impute routine, which targets the same convex program
as eq. (2) (loss \verb{sum_js w (Y - alpha - beta - L)^2}, with no 1/2 factor);
\code{lambda_nn} is therefore on the same scale as the official Python and Stata
packages, and finite-penalty results agree with them to solver tolerance.

\code{trop_matrix()} always uses the pooled weighting scheme anchored to the centre
of the treated block: it takes no \code{anchor} argument and has no per-cell mode.
It therefore computes the same pooled, block-centre estimator as the official
Python package's \code{TROP_TWFE_average} (implemented independently), and exists as
a like-for-like matrix-in cross-check against that pooled reference.
\code{trop(..., anchor = "pooled")} uses the same convention (unit distances to the
\emph{average} treated trajectory, time distances to the centre of the treated
block), so on a trailing-block design the two agree to solver tolerance for
the same penalties. For the per-cell estimator (the paper's eq. (12) /
Algorithm 2), use \code{trop(..., anchor = "per_cell")}, which shares the
data-frame core \code{.trop_att()}.
}
\examples{
df <- sim_panel(N = 15, T = 12, n_treated = 3, t0 = 9, att = 2, seed = 1)
Y <- matrix(0, max(df$id), max(df$t)); W <- Y
for (k in seq_len(nrow(df))) { Y[df$id[k], df$t[k]] <- df$y[k]
  W[df$id[k], df$t[k]] <- df$w[k] }
tu <- which(rowSums(W) > 0)
trop_matrix(Y, W, tu, 0.1, 0.1, Inf, treated_periods = 4)

# treated_units and treated_periods are inferred from W when omitted, so the
# matrices produced by panel_matrices() can be fed in directly.
pm <- panel_matrices(df, "y", "w", "id", "t")
trop_matrix(pm$Y, pm$W, lambda_unit = 0.1, lambda_time = 0.1, lambda_nn = Inf)
}
\references{
Athey, S., Imbens, G. W., Qu, Z., & Viviano, D. (2026).
\emph{Triply Robust Panel Estimators.} Journal of Applied Econometrics. \doi{10.1002/jae.70061}.
}
\seealso{
\code{\link[=trop]{trop()}}, \code{\link[=panel_compare]{panel_compare()}}
}
