% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_panel}
\alias{sim_panel}
\title{Simulate a panel from a low-rank factor model}
\usage{
sim_panel(
  N = 30,
  T = 20,
  n_treated = 5,
  t0 = NULL,
  rank = 3L,
  att = 1,
  noise = 0.5,
  seed = NULL,
  n_cov = 0L,
  phi = NULL,
  confounding = 0
)
}
\arguments{
\item{N}{Number of units.}

\item{T}{Number of periods.}

\item{n_treated}{Number of treated units.}

\item{t0}{First treated period (block design).}

\item{rank}{Number of latent factors.}

\item{att}{True treatment effect added to treated cells.}

\item{noise}{Standard deviation of idiosyncratic noise.}

\item{seed}{Optional RNG seed.}

\item{n_cov}{Number of time-varying covariates to generate (default \code{0},
none). Generated covariates are cell-level standard-normal draws whose
linear index is part of the untreated potential outcome \code{Y(0)}, matching the
additive covariate model of \code{\link[=trop]{trop()}} (\code{L = X.phi + R}); pass their names to
\code{covariates} in \code{\link[=trop]{trop()}} / \code{\link[=panel_compare]{panel_compare()}} to recover the coefficients.
Defaults to \code{length(phi)} when \code{phi} is supplied.}

\item{phi}{Optional numeric coefficient vector for the covariates (length
\code{n_cov}). When \code{NULL} and \code{n_cov > 0}, coefficients are drawn from a
standard normal. The coefficients actually used are returned on the
\code{"phi"} attribute of the result.}

\item{confounding}{Non-negative strength of selection-on-confounders in the
treatment assignment. With \code{confounding = 0} (default) the first
\code{n_treated} units are treated independently of the latent factors and
covariates, so treatment is ignorable and DID/TWFE is unbiased. With
\code{confounding > 0} treated units are selected on their latent factor
loadings and, when covariates are present (\code{n_cov > 0}), on their
post-period covariate index \eqn{X'\phi} as well. The factor part biases
estimators without a low-rank term (DID, SDID, SC); the covariate part
biases estimators that ignore covariates (SDID, SC, uncontrolled DID). A
fully robust estimator (e.g. TROP with \verb{covariates=}) removes both. Larger
values give stronger selection.}
}
\value{
A long \code{data.frame} with columns \code{id}, \code{t}, \code{y}, \code{w}, and the noiseless
counterfactual \code{y0} (useful for evaluating estimators). When \code{n_cov > 0} it
additionally carries covariate columns \code{x1}, ..., \verb{x\{n_cov\}}, and the true
coefficients are stored on \code{attr(., "phi")}.
}
\description{
Generates a long panel whose untreated potential outcomes follow an
interactive-fixed-effects (factor) model on top of two-way fixed effects,
in the spirit of the data-generating processes in Athey, Imbens, Qu &
Viviano (2025). A block treatment is applied to \code{n_treated} units from period
\code{t0} onward, with a constant additive effect \code{att}.
}
\examples{
df <- sim_panel(N = 30, T = 15, n_treated = 5, t0 = 11, seed = 42)
head(df)

# Two covariates with known coefficients, recoverable by trop(covariates=):
dc <- sim_panel(N = 30, T = 12, n_treated = 4, t0 = 9,
                n_cov = 2, phi = c(1.2, -0.7), seed = 1)
attr(dc, "phi")
}
