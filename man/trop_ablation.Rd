% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ablation.R
\name{trop_ablation}
\alias{trop_ablation}
\alias{print.trop_ablation}
\alias{plot.trop_ablation}
\title{Penalty ablation for the TROP estimator}
\usage{
trop_ablation(
  data,
  outcome,
  treatment,
  unit,
  time,
  covariates = NULL,
  control = trop_control(),
  anchor = "pooled",
  se = "none",
  lambda_full = NULL,
  rmse = FALSE,
  horizon = NULL,
  n_pseudo = NULL,
  n_runs = 200L
)

\method{print}{trop_ablation}(x, digits = 3, ...)

\method{plot}{trop_ablation}(x, file = NULL, width = NULL, height = NULL, res = 200, digits = 3, ...)
}
\arguments{
\item{data, outcome, treatment, unit, time, covariates}{Passed to \code{\link[=trop]{trop()}}.}

\item{control}{A \code{\link[=trop_control]{trop_control()}} list (used for every fit).}

\item{anchor}{Weight anchoring, as in \code{\link[=trop]{trop()}} (default \code{"pooled"}).}

\item{se}{Standard-error method for every specification (default \code{"none"};
any \code{\link[=trop]{trop()}} method is allowed, e.g. \code{"jackknife"}). When not \code{"none"}, the
standard error and confidence interval appear in the table.}

\item{lambda_full}{Optional named list \code{list(time=, unit=, nn=)} to fix the
full specification's penalties and skip cross-validation.}

\item{rmse}{Logical; if \code{TRUE}, also score each specification by a placebo
RMSE. A pseudo block-treatment (true effect zero) is assigned to control
units, every specification is refit at its penalties, and the RMSE is
\code{sqrt(mean(estimate^2))} over \code{n_runs} draws. The same draws are scored under
every specification, so the relative RMSE with TROP (full) = 1 is the
quantity reported in Table 5 of the paper. Adds \code{rmse} and \code{rmse_rel}
columns, and makes \code{plot()} show the (relative) RMSE instead of the ATT.}

\item{horizon, n_pseudo}{Placebo design used when \code{rmse = TRUE}: the number of
final periods to pseudo-treat and the number of pseudo-treated control
units. Default to the real design's post length and treated-unit count.}

\item{n_runs}{Number of placebo draws when \code{rmse = TRUE}.}

\item{x}{A \code{trop_ablation} object.}

\item{digits}{Number of decimal places for the penalties and estimates.}

\item{...}{Unused.}

\item{file}{Optional output path. When supplied, the table is rendered to that
file instead of the active graphics device; the format is taken from the
extension (\code{.png}, the default, or \code{.pdf} for vector output). For LaTeX or
Markdown source use \code{\link[=format]{format()}} instead.}

\item{width, height}{Figure size in inches. Defaults adapt to the number of
rows and columns.}

\item{res}{Resolution in PPI for the \code{.png} device (ignored for \code{.pdf}).}
}
\value{
An object of class \code{trop_ablation} (a \code{data.frame}) with one row per
specification: \code{spec}, the three penalties \code{lambda_time}/\code{lambda_unit}/
\code{lambda_nn}, \code{estimate}, \code{std.error}, \code{conf.low}/\code{conf.high}, and \code{rank}.
When \code{rmse = TRUE} it also has \code{rmse} and \code{rmse_rel} (RMSE relative to the
full TROP).
}
\description{
Refits TROP under a sequence of penalty constraints to show how the ATT moves
as the estimator is stripped back towards matrix completion and
difference-in-differences. This is the robustness exercise behind Table 5 of
Athey, Imbens, Qu & Viviano (2026): constraining \code{lambda_unit} and/or
\code{lambda_time} to zero (dropping unit/time weights) and/or \code{lambda_nn} to
infinity (dropping the low-rank regression adjustment).
}
\details{
The full specification's penalties are taken from a single cross-validated
\code{\link[=trop]{trop()}} fit; every constrained specification then reuses those values where
they are not constrained, so the rows are directly comparable. The result is a
table object: print it for a clean console table, or \code{\link[=format]{format()}} it to
paste-ready LaTeX (booktabs) or Markdown. It is also an ordinary
\code{data.frame}, so it can be passed to \code{knitr::kable()}, \code{gt::gt()}, etc.
}
\examples{
df <- sim_panel(N = 14, T = 8, n_treated = 3, t0 = 5, att = 2, seed = 1)
# fixed base penalties keep the example fast; omit `lambda_full` to select
# them by cross-validation
ab <- trop_ablation(df, "y", "w", "id", "t",
                    lambda_full = list(time = 0.1, unit = 0.5, nn = 2))
ab                                   # clean console table
\donttest{
cat(format(ab, "latex"), sep = "\n") # paste-ready LaTeX (booktabs)
}
}
\references{
Athey, S., Imbens, G. W., Qu, Z., & Viviano, D. (2026).
Triply Robust Panel Estimators. \emph{Journal of Applied Econometrics}. \doi{10.1002/jae.70061}.
}
\seealso{
\code{\link[=trop]{trop()}}; \code{format()} for paste-ready LaTeX/Markdown output
}
