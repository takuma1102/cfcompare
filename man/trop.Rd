% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trop.R
\name{trop}
\alias{trop}
\title{Triply RObust Panel (TROP) estimator}
\usage{
trop(
  data,
  outcome,
  treatment,
  unit,
  time,
  covariates = NULL,
  lambda = NULL,
  anchor = c("auto", "per_cell", "pooled"),
  se = c("bootstrap", "auto", "jackknife", "placebo", "none"),
  grids = NULL,
  control = trop_control(),
  verbose = FALSE
)
}
\arguments{
\item{data}{A long \code{data.frame} with one row per unit-time.}

\item{outcome, treatment, unit, time}{Column names (strings). \code{treatment} must be
a 0/1 indicator of \emph{active} treatment in that cell (works for block,
staggered, and non-absorbing designs).}

\item{covariates}{Optional character vector of column names for time-varying
covariates. When supplied, the low-rank term is augmented additively as
\eqn{L_{js} = X_{js}'\phi + R_{js}} (Athey, Imbens, Qu & Viviano 2025,
Section 6.2): the covariate-linear part is unpenalised and the nuclear norm
applies to the residual \eqn{R}. Penalty cross-validation is performed on the
covariate-residualised model. Covariates must be fully observed (including in
treated cells, where they enter the counterfactual). The fitted coefficients
are returned in \verb{$phi}.}

\item{lambda}{Optional named list \code{list(time=, unit=, nn=)} to fix the
penalties and skip cross-validation.}

\item{anchor}{How weights are anchored to treated cells: \code{"per_cell"}
re-solves eq. (2) with cell-specific weights for every treated cell
(faithful to the paper); \code{"pooled"} solves once with weights anchored to the
treated set (fast); \code{"auto"} (default) uses \code{per_cell} when there are at
most \code{max_cells} treated cells and \code{pooled} otherwise.}

\item{se}{Standard-error method: \code{"bootstrap"} (default; unit-level
stratified block bootstrap), \code{"jackknife"} (leave-one-treated-unit-out;
needs at least 2 treated units), \code{"placebo"} (assign the treated pattern to
controls; for a single treated unit), \code{"auto"} (jackknife when there are at
least 2 treated units, else placebo), or \code{"none"}.}

\item{grids}{Optional list of penalty grids; see \code{\link[=trop_control]{trop_control()}}.}

\item{control}{A list of solver/CV settings from \code{\link[=trop_control]{trop_control()}}.}

\item{verbose}{Logical; print CV progress.}
}
\value{
An object of class \code{trop}: a list with the ATT \code{estimate},
\code{std.error}, \code{conf.low}/\code{conf.high}, the selected penalties \code{lambda},
any covariate coefficients \code{phi}, per-cell effects, the estimated
counterfactual matrix, weights, and the reshaped panel.
}
\description{
Fits the TROP estimator of Athey, Imbens, Qu & Viviano (2025) on a long
panel. TROP combines a low-rank-plus-two-way-fixed-effects outcome model with
exponential-decay unit weights (upweighting controls similar to the treated)
and time weights (upweighting periods near the treated periods). Penalties are
chosen by leave-one-out cross-validation on the control cells. The estimator
nests DID/TWFE, matrix completion and synthetic-control-type weighting as
special cases.
}
\examples{
df <- sim_panel(N = 20, T = 12, n_treated = 4, t0 = 9, seed = 1)
fit <- trop(df, "y", "w", "id", "t", se = "none",
            control = trop_control(n_cv_cells = 8L, cv_cycles = 1L))
fit
}
\references{
Athey, S., Imbens, G. W., Qu, Z., & Viviano, D. (2025).
Triply Robust Panel Estimators. arXiv:2508.21536.
}
