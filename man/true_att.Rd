% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{true_att}
\alias{true_att}
\title{True ATT of a (semi-)synthetic panel}
\usage{
true_att(data, treatment = "w", tau = "tau")
}
\arguments{
\item{data}{A long \code{data.frame} with a treatment indicator and either a \code{tau}
column (true per-cell effect) or both \code{y} and \code{y0} columns.}

\item{treatment}{Name of the 0/1 treatment column (default \code{"w"}).}

\item{tau}{Name of the true-effect column (default \code{"tau"}). When that column
is absent the per-cell effect is taken as \code{y - y0}.}
}
\value{
A single numeric: the true ATT averaged over treated cells.
}
\description{
Convenience accessor for the ground-truth average treatment effect on the
treated of a simulated panel. It reads the per-cell true effect --- the \code{tau}
column produced by \code{\link[=sim_semisynthetic]{sim_semisynthetic()}}, or \code{y - y0} for a \code{\link[=sim_panel]{sim_panel()}}
panel --- and averages it over the actively treated cells, giving the number
estimators are trying to recover. Pair it with \code{\link[=panel_compare]{panel_compare()}} or
\code{\link[=panel_rmse]{panel_rmse()}} to score estimates against the truth without re-deriving it by
hand.
}
\examples{
real <- sim_panel(N = 16, T = 10, n_treated = 0L, att = 0, seed = 1)
ss <- sim_semisynthetic(real, "y", "id", "t",
                        n_treated = 6, t0 = 14, att = 3, seed = 2)
true_att(ss)   # 3
}
\seealso{
\code{\link[=sim_semisynthetic]{sim_semisynthetic()}}, \code{\link[=sim_panel]{sim_panel()}}, \code{\link[=panel_compare]{panel_compare()}}
}
