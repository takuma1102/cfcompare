% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmse_curve.R
\name{rmse_curves}
\alias{rmse_curves}
\title{Estimation-RMSE curves over both design dimensions}
\usage{
rmse_curves(values_control = NULL, values_pre = NULL, ...)
}
\arguments{
\item{values_control, values_pre}{Optional grids for each sweep (see
\code{\link[=rmse_curve]{rmse_curve()}} defaults).}

\item{...}{Passed to \code{\link[=rmse_curve]{rmse_curve()}} (\code{n_runs}, \code{methods}, base design, \code{att},
\code{noise}, \code{control}, \code{seed}, \code{verbose}, ...).}
}
\value{
A \code{cf_rmse_curves} object: a list with \verb{$n_control} and \verb{$n_pre},
each a \code{cf_rmse_curve}.
}
\description{
Convenience wrapper that runs \code{\link[=rmse_curve]{rmse_curve()}} for both the number of control
units and the number of pre-treatment periods and bundles the two curves, so
they can be plotted individually (default) or side by side (\code{combined = TRUE}),
as in the paper's two-panel figure.
}
\examples{
\donttest{
# defaults are a large simulation; use a small grid + few reps for a quick look
g <- rmse_curves(values_control = seq(30, 90, by = 10),
                 values_pre = seq(6, 30, by = 4), n_runs = 30)
plot(g)                  # two separate figures (default)
plot(g, combined = TRUE) # side-by-side, paper-style
}
}
\seealso{
\code{\link[=rmse_curve]{rmse_curve()}}, \code{\link[=autoplot.cf_rmse_curves]{autoplot.cf_rmse_curves()}}
}
