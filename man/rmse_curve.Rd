% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmse_curve.R
\name{rmse_curve}
\alias{rmse_curve}
\title{Estimation-RMSE curve over one design dimension}
\usage{
rmse_curve(
  vary = c("n_control", "n_pre"),
  values = NULL,
  n_runs = 500L,
  methods = c("DID", "SDID", "SC", "MC", "DIFP", "TROP"),
  n_control = 60L,
  n_treated = 8L,
  n_pre = 16L,
  n_post = 6L,
  rank = 4L,
  att = 2,
  noise = 1,
  ar = 0.4,
  trend_sd = 0.05,
  anchor = "pooled",
  control = trop_control(),
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{vary}{Which dimension to sweep: \code{"n_control"} or \code{"n_pre"}.}

\item{values}{Integer vector of values for the swept dimension. Defaults to
\code{seq(20, 45, by = 5)} for control units and \code{seq(5, 38, by = 2)} for
pre-periods. Each value is an actual measured point (its own Monte Carlo);
pass a denser/wider grid for smoother or longer curves.}

\item{n_runs}{Monte Carlo replications per value (higher = smoother; the paper
uses ~1000). Default 500. \strong{Note:} with the dense default grid and six
estimators this is a large simulation (thousands of fits per panel); reduce
\code{n_runs} and/or \code{values} for a quick look, or parallelise the outer loop.}

\item{methods}{Estimators to include; any subset of the \code{\link[=panel_compare]{panel_compare()}}
methods. Default \code{c("DID", "SDID", "SC", "MC", "DIFP", "TROP")}, the six
estimators compared in the paper. \code{gsynth}, \code{augsynth} and \code{CS} can be added
if those packages are installed.}

\item{n_control, n_treated, n_pre, n_post}{Base design; the dimension named in
\code{vary} is overridden by \code{values}, the rest are held fixed. Defaults give a
sizeable panel (60 controls, 8 treated, 16 pre- and 6 post-periods).}

\item{rank, att, noise}{Number of latent factors, the imposed (true) ATT, and
the idiosyncratic-noise scale.}

\item{ar}{AR(1) coefficient of the idiosyncratic errors (serial correlation).}

\item{trend_sd}{SD of the heterogeneous unit-specific linear trend slopes
(non-parallel trends); treatment is also selected on this slope.}

\item{anchor}{Estimation anchor for the native ATT (\code{"pooled"} by default for
speed across the many fits).}

\item{control}{Solver/CV controls from \code{\link[=trop_control]{trop_control()}}.}

\item{seed}{Base integer seed (each replication uses a distinct offset).}

\item{verbose}{Logical; print progress per swept value.}
}
\value{
A \code{cf_rmse_curve} (a \code{data.frame}) with columns \code{method}, \code{x}
(the swept value), \code{rmse}, \code{bias}, \code{n_runs}. The swept-dimension label is in
\code{attr(., "vary")}.
}
\description{
Sweeps one design dimension -- the number of control units (\code{"n_control"}) or
the number of pre-treatment periods (\code{"n_pre"}) -- and, at each value, runs a
semi-synthetic Monte Carlo: panels are drawn from a latent factor model in
which treatment is selected on the factor loadings (so plain DID/TWFE is
biased), a known constant effect \code{att} is imposed, every estimator is run, and
the estimation RMSE against the known truth is recorded. The result is the
data behind the paper-style "RMSE vs. N_control / T_pre" line plot, one line
per estimator. With a dense \code{values} grid and enough \code{n_runs} the curves are
smooth, as in the paper.
}
\details{
This is a simulation diagnostic on synthetic data; it does not take a user
panel. To run a curve on your own data, draw panels with \code{\link[=sim_semisynthetic]{sim_semisynthetic()}}
and call \code{\link[=panel_compare]{panel_compare()}} in a loop.
}
\examples{
\donttest{
# quick look (small grid + few reps); raise n_runs/values for paper quality
cc <- rmse_curve("n_control", values = seq(30, 90, by = 10), n_runs = 30)
autoplot(cc)
}
}
\seealso{
\code{\link[=rmse_curves]{rmse_curves()}}, \code{\link[=panel_rmse]{panel_rmse()}}, \code{\link[=autoplot.cf_rmse_curve]{autoplot.cf_rmse_curve()}}
}
