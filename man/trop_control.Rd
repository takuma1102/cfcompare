% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trop.R
\name{trop_control}
\alias{trop_control}
\title{Control settings for \code{\link[=trop]{trop()}}}
\usage{
trop_control(
  max_iter = 2000L,
  tol = 1e-06,
  n_cv_cells = 120L,
  cv_cycles = 2L,
  max_cells = 60L,
  conf_level = 0.95,
  n_boot = 200L,
  boot_ci = c("percentile", "normal"),
  svd = c("truncated", "full"),
  cv_method = c("loocv", "placebo"),
  n_placebo = 30L,
  nn_floor = "auto",
  nn_floor_scale = 0.001,
  workers = 1L,
  seed = NULL
)
}
\arguments{
\item{max_iter}{Maximum solver iterations. Higher by default because small
\code{lambda_nn} converges slowly under the soft-impute iteration.}

\item{tol}{Reference solver convergence tolerance. The applied tolerance is
tightened automatically as \code{lambda_nn} weakens (small penalties converge
slowly), keeping accuracy roughly constant across the penalty grid; a fixed
tolerance otherwise under-converges at small \code{lambda_nn}. See \code{\link[=trop]{trop()}}.}

\item{n_cv_cells}{Number of control cells sampled for the CV criterion. Set
to \code{Inf} (or \code{0}) to use \emph{every} control cell, i.e. the paper's full
eq. (5) criterion (the Stata command's \code{cells(0)}); slower but exact.}

\item{cv_cycles}{Number of coordinate-descent cycles in penalty selection.}

\item{max_cells}{Threshold for \code{anchor = "auto"} to switch to pooled weights.}

\item{conf_level}{Confidence level for intervals.}

\item{n_boot}{Number of replications for the bootstrap standard error
(\code{se = "bootstrap"}).}

\item{boot_ci}{Bootstrap confidence-interval type: \code{"percentile"} or
\code{"normal"}.}

\item{svd}{Singular-value decomposition used by the soft-impute solver:
\code{"truncated"} (default) computes only the leading singular triplets with
\code{RSpectra} when it is installed and the matrix is large enough to benefit,
falling back to the full SVD otherwise; \code{"full"} always uses the exact base
R \code{svd()}. The two agree to numerical tolerance; \code{"truncated"} is faster on
large panels, \code{"full"} is used for exact numerical-agreement checks.}

\item{cv_method}{Penalty cross-validation criterion. \code{"loocv"} (default)
scores held-out control-cell prediction error (the paper's eq. (4)-(5));
\code{"placebo"} instead stamps the \emph{actual} treated-unit adoption patterns
onto randomly resampled control units and minimises the mean squared
placebo ATT (the placebo-RMSE / pattern-resampling criterion of the
official Python and Stata packages). Under block adoption the two placebo
variants coincide; under staggered adoption each placebo draw reproduces
the real cohort structure, so \code{"placebo"} is the recommended choice there
(a warning is emitted if \code{"loocv"} is used on a staggered design).
Ignored when the penalties are supplied via \code{lambda} (CV is then
skipped). The placebo criterion always uses the pooled (per-cohort
block-centre, cell-weighted) ATT, even when the final fit uses
\code{anchor = "per_cell"}.}

\item{n_placebo}{Number of placebo assignments drawn when
\code{cv_method = "placebo"} (larger is more stable but slower).}

\item{nn_floor}{Small non-negative stabilising constant \emph{added} to every
finite \code{lambda_nn} inside the solver (\code{lambda_nn = Inf}, the DID/TWFE
special case, is never affected). Rationale: the nuclear-norm proximal
step soft-thresholds singular values by \verb{lambda_nn / (2 max w)}, so at
\code{lambda_nn = 0} the step degenerates to the identity, the completion of
the treated cells is no longer pinned down by the program, and soft-impute
converges slowly; a strictly positive floor keeps the
weak-regularisation limit well posed. The default \code{"auto"} resolves to
\code{nn_floor_scale * s1}, where \code{s1} is the operator norm (largest singular
value) of the two-way-demeaned control outcome -- the same data-scale
reference the default \code{lambda_nn} grid is built from. The floor is
therefore \emph{relative to the outcome scale}: it is resolved on the fitting
scale, so it adapts automatically when \code{standardize = TRUE} (both modes
get the same floor in relative terms). Supply a single non-negative
number to use that exact value instead (interpreted on the fitting scale,
like \code{lambda}), or \code{0} to disable the adjustment entirely, in which case
penalties are applied exactly as given -- use \code{nn_floor = 0} for
like-for-like numerical comparisons against other implementations at
identical penalty values. The resolved value is stored in the fit as
\verb{$lambda$nn_floor} and printed when positive.}

\item{nn_floor_scale}{Relative constant used when \code{nn_floor = "auto"}: the
resolved floor is \code{nn_floor_scale * s1} (default \code{1e-3}, i.e. 0.1\\% of the
data's operator norm -- an epsilon next to the default \code{lambda_nn} grid,
whose smallest finite point is \code{0.04 * s1}). Ignored when \code{nn_floor} is
numeric.}

\item{workers}{Number of parallel workers for the embarrassingly parallel
loops (cross-validation cells, and the bootstrap / jackknife
replicates). \code{1} (default) runs serially. Values \verb{> 1} use
\code{future.apply}/\code{future} when installed (a transient \code{multisession} plan is
set up and restored automatically); if those packages are missing it falls
back to serial with no error. Results are reproducible given \code{seed}.}

\item{seed}{Optional seed for CV cell sampling (reproducibility).}
}
\value{
A list of control parameters.
}
\description{
Control settings for \code{\link[=trop]{trop()}}
}
