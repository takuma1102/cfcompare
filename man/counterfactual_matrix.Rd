% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counterfactual.R
\name{counterfactual_matrix}
\alias{counterfactual_matrix}
\alias{predict_counterfactual}
\title{Estimated counterfactual (untreated potential outcome) matrix}
\usage{
counterfactual_matrix(object, ...)

predict_counterfactual(object, ...)
}
\arguments{
\item{object}{A fitted object: a \code{trop} fit, a \code{cf_comparison} from
\code{\link[=panel_compare]{panel_compare()}}, a \pkg{synthdid} estimate, or any object carrying a
\code{counterfactual} element.}

\item{...}{Unused; for method extensibility.}
}
\value{
For a single fit, an \verb{N x T} numeric matrix (with \code{NA} where the
method does not define a counterfactual). For a \code{cf_comparison}, a named
list of such matrices, one per method.
}
\description{
Returns a method's estimate of the untreated potential outcome \eqn{Y(0)} as
an \verb{N x T} matrix on the panel's unit/time grid. This gives every estimator a
common interface, so counterfactuals -- and the unit-period prediction RMSE
built on them (\code{\link[=panel_rmse]{panel_rmse()}} with \code{metric = "prediction"}) -- can be compared
on the same footing.
}
\details{
Coverage of the returned matrix differs by method. The native engines
(\code{trop()}, and the DID/MC/TROP fits inside \code{\link[=panel_compare]{panel_compare()}}) return a fitted
value for \emph{every} cell. Methods whose model only defines a counterfactual for
the treated units -- DIFP, and (when the optional package is installed) SDID,
SC and gsynth -- fill the treated rows and leave control cells \code{NA} (a
control's untreated outcome is simply its observed value).

\code{predict_counterfactual()} is an alias for \code{counterfactual_matrix()}.
}
\examples{
df <- sim_panel(N = 14, T = 9, n_treated = 3, t0 = 6, seed = 1)
fit <- trop(df, "y", "w", "id", "t",
            lambda = list(time = 0.1, unit = 0.5, nn = 2), se = "none")
M <- counterfactual_matrix(fit)
dim(M)
}
\seealso{
\code{\link[=panel_rmse]{panel_rmse()}}, \code{\link[=trop]{trop()}}, \code{\link[=panel_compare]{panel_compare()}}
}
