% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmse.R
\name{panel_rmse}
\alias{panel_rmse}
\title{Out-of-sample RMSE across panel estimators}
\usage{
panel_rmse(
  data,
  outcome,
  treatment,
  unit,
  time,
  methods = c("DID", "SC", "SDID", "MC", "TROP", "DIFP"),
  exclude = NULL,
  metric = c("placebo", "prediction"),
  horizon = 10L,
  n_pseudo = 10L,
  n_runs = 10L,
  control = trop_control(),
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A long \code{data.frame}, one row per unit-time.}

\item{outcome, treatment, unit, time}{Column names (strings).}

\item{methods}{Methods to compare; subset of \code{"DID"}, \code{"MC"}, \code{"TROP"},
\code{"DIFP"}, \code{"SDID"}, \code{"SC"}, \code{"gsynth"}, \code{"augsynth"}, \code{"CS"}. Defaults to
\code{c("DID", "SC", "SDID", "MC", "TROP", "DIFP")}.}

\item{exclude}{Optional character vector of methods to drop from \code{methods}
(e.g. \code{exclude = "DIFP"}). Unknown names are ignored with a warning.}

\item{metric}{\code{"placebo"} (placebo-ATT RMSE, all methods) or \code{"prediction"}
(per-cell held-out counterfactual RMSE for \code{"DID"}/\code{"MC"}/\code{"TROP"}/\code{"DIFP"},
and \code{"SDID"}/\code{"SC"} when \pkg{synthdid} is installed; \code{"gsynth"},
\code{"augsynth"} and \code{"CS"} are \code{NA} for this metric).}

\item{horizon}{Number of final periods held out per placebo cohort.}

\item{n_pseudo}{Number of placebo (pseudo-treated) control units per run.}

\item{n_runs}{Number of placebo runs to average over.}

\item{control}{A list of solver/CV controls from \code{\link[=trop_control]{trop_control()}}.}

\item{seed}{Optional integer seed for reproducible placebo draws.}

\item{verbose}{Logical; print progress.}
}
\value{
A \code{cf_rmse_tbl} (a \code{data.frame}) with one row per method and columns
\code{method}, \code{rmse}, \code{rmse_se}, \code{n_runs}, \code{engine}, \code{note}.
}
\description{
Compares estimators by how well each does on held-out control data, following
the "random blocks" placebo idea of the doubly/triply robust panel estimator
paper. Two scoring rules are available:
}
\details{
\itemize{
\item \code{metric = "placebo"} (default): in each run a random set of control units is
given a \emph{placebo} block treatment in the final \code{horizon} periods (true effect
zero), every method is estimated on that control-only panel, and the score is
\code{sqrt(mean(ATT^2))} across runs. This works for \strong{every} method -- native
(DID, MC, TROP) and wrapped (\code{SDID}/\code{SC} via \pkg{synthdid}, \code{gsynth},
\code{augsynth}, \code{CS} = Callaway & Sant'Anna via \pkg{did}).
\item \code{metric = "prediction"}: per-cell one-step-ahead held-out RMSE (the paper's
Table-1 unit-period counterfactual prediction error). A block of control
cells is held out and each method predicts the untreated outcome there via
its counterfactual matrix (\code{\link[=counterfactual_matrix]{counterfactual_matrix()}}). Supported for the
native methods (\code{DID}, \code{MC}, \code{TROP}), for \code{DIFP}, and -- when \pkg{synthdid}
is installed -- for \code{SDID} and \code{SC}. The remaining wrapped methods
(\code{gsynth}, \code{augsynth}, \code{CS}) are reported as \code{NA} for this metric; use
\code{metric = "placebo"} for them.
}

Lower is better. Native methods are tuned once on the real data (DID is
parameter-free; MC selects \code{lambda_nn}; TROP selects the full triplet by
cross-validation) and reused across runs. Wrapped methods are skipped with a
note when their package is missing or the design does not apply.
}
\examples{
\donttest{
df <- sim_panel(N = 20, T = 12, n_treated = 4, t0 = 9, att = 2, seed = 1)
r <- panel_rmse(df, "y", "w", "id", "t",
                methods = c("DID", "TROP"),
                horizon = 2, n_pseudo = 3, n_runs = 2,
                control = trop_control(n_cv_cells = 8L, cv_cycles = 1L),
                seed = 1)
r
autoplot(r)
}
}
\references{
Athey, S., Imbens, G. W., Qu, Z., & Viviano, D. (2025/2026).
Triply/Doubly Robust Panel Estimators.
}
\seealso{
\code{\link[=panel_compare]{panel_compare()}}, \code{\link[=autoplot.cf_rmse_tbl]{autoplot.cf_rmse_tbl()}}
}
