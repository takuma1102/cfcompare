% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{panel_compare}
\alias{panel_compare}
\title{Compare DID, SDID, MC and TROP on the same panel}
\usage{
panel_compare(
  data,
  outcome,
  treatment,
  unit,
  time,
  methods = c("DID", "SDID", "MC", "TROP", "DIFP"),
  exclude = NULL,
  anchor = "auto",
  se = c("auto", "jackknife", "bootstrap", "placebo", "none"),
  control = trop_control(),
  verbose = FALSE
)
}
\arguments{
\item{data}{A long \code{data.frame} with one row per unit-time.}

\item{outcome, treatment, unit, time}{Column names (strings). \code{treatment} must be
a 0/1 indicator of \emph{active} treatment in that cell (works for block,
staggered, and non-absorbing designs).}

\item{methods}{Character vector of methods to run. Any of \code{"DID"}, \code{"SDID"},
\code{"SC"}, \code{"MC"}, \code{"DIFP"}, \code{"TROP"}, \code{"gsynth"}, \code{"augsynth"}, \code{"CS"}.
Defaults to the native engines plus SDID: \code{c("DID", "SDID", "MC", "TROP", "DIFP")}.}

\item{exclude}{Optional character vector of methods to drop from \code{methods}
(after defaults are applied). Convenient for running "everything except one",
e.g. \code{exclude = "DIFP"}. Unknown names are ignored with a warning.}

\item{anchor}{Weight anchoring for TROP; see \code{\link[=trop]{trop()}}.}

\item{se}{Standard-error method for the native engines; see \code{\link[=trop]{trop()}}.}

\item{control}{A list of solver/CV settings from \code{\link[=trop_control]{trop_control()}}.}

\item{verbose}{Logical; print CV progress.}
}
\value{
An object of class \code{cf_comparison}: a list with the tidy table
\code{att} (class \code{cf_att_tbl}), per-method counterfactual matrices
\code{counterfactual}, the native fit objects \code{fits}, and the reshaped \code{panel}.
}
\description{
Runs a set of panel estimators on a single long panel and returns their
average-treatment-effect-on-the-treated (ATT) estimates on a common tidy
schema, so applied researchers can compare them at a glance. DID, MC and TROP
are computed natively (no external dependencies); SDID and SC are routed
through the \pkg{synthdid} package when available, and an alternative MC/IFE
can be routed through \pkg{gsynth}. Methods whose optional package is missing,
or that do not apply to the design, are skipped with a message.
}
\examples{
df <- sim_panel(N = 25, T = 14, n_treated = 4, t0 = 10, seed = 3)
cmp <- panel_compare(df, "y", "w", "id", "t",
                     methods = c("DID", "MC", "TROP"), se = "none",
                     control = trop_control(n_cv_cells = 8L, cv_cycles = 1L))
cmp$att
}
\seealso{
\code{\link[=trop]{trop()}}, \code{\link[=autoplot.cf_comparison]{autoplot.cf_comparison()}}, \code{\link[=plot_counterfactual]{plot_counterfactual()}}
}
