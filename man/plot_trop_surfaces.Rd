% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{plot_trop_surfaces}
\alias{plot_trop_surfaces}
\title{Base-R surface views of a TROP penalty-sensitivity grid}
\usage{
plot_trop_surfaces(
  grid,
  which = c("both", "cv_loss", "att"),
  ask = interactive(),
  ...
)
}
\arguments{
\item{grid}{A \code{cf_trop_grid} returned by \code{\link[=trop_sensitivity]{trop_sensitivity()}}.}

\item{which}{Which surface(s) to draw: \code{"both"} (default), \code{"cv_loss"}, or
\code{"att"}. With \code{"both"} the two surfaces are drawn as separate figures; on an
interactive screen device you are prompted between them (use a multi-page
device such as \code{pdf()}, or call once per surface, to keep both).}

\item{ask}{Logical; when drawing both on an interactive device, prompt before
the second figure. Defaults to \code{interactive()}.}

\item{...}{Further arguments passed to \code{\link[graphics:filled.contour]{graphics::filled.contour()}}.}
}
\value{
Invisibly, a list with the \code{cv_loss} and \code{att} surface matrices
(rows = \code{lambda_time}, columns = \code{lambda_nn}).
}
\description{
Draws the cross-validation-loss surface and the ATT surface from a
\code{\link[=trop_sensitivity]{trop_sensitivity()}} grid as two separate, full-width base-graphics plots,
each with a colour key (legend) on the right. Unlike \code{\link[=autoplot.cf_trop_grid]{autoplot.cf_trop_grid()}}
--- a single \code{ggplot2} heatmap that packs both into one panel --- this view
keeps the surfaces apart so neither is squeezed, and needs no \code{ggplot2}.
}
\examples{
\donttest{
df <- sim_panel(N = 20, T = 12, n_treated = 4, t0 = 9, att = 2, seed = 1)
g <- trop_sensitivity(df, "y", "w", "id", "t",
                      lambda_time = c(0, 0.25, 1),
                      control = trop_control(n_cv_cells = 12L, cv_cycles = 1L))
plot_trop_surfaces(g, which = "cv_loss")
}
}
\seealso{
\code{\link[=trop_sensitivity]{trop_sensitivity()}}, \code{\link[=autoplot.cf_trop_grid]{autoplot.cf_trop_grid()}}
}
