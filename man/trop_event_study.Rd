% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trop.R
\name{trop_event_study}
\alias{trop_event_study}
\title{Per-period (event-study) effects from a TROP fit}
\usage{
trop_event_study(
  object,
  se = c("bootstrap", "jackknife", "none"),
  pre_periods = TRUE,
  control = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{trop} fit from \code{\link[=trop]{trop()}}.}

\item{se}{Standard-error method for the per-period effects: \code{"bootstrap"}
(default; unit-level stratified block bootstrap), \code{"jackknife"}
(leave-one-treated-unit-out; needs at least 2 treated units), or \code{"none"}.}

\item{pre_periods}{Logical; include pre-treatment event times as placebo /
pre-trend points (default \code{TRUE}).}

\item{control}{A list of solver/CV/bootstrap settings from \code{\link[=trop_control]{trop_control()}};
defaults to \code{trop_control()}. Use it to set \code{n_boot}, \code{boot_ci}, \code{seed}, and
\code{workers} for the resampling.}

\item{...}{Unused.}
}
\value{
An object of class \code{trop_event_study}: a list whose \code{estimates} is a
\code{data.frame} with one row per event time (\code{event_time}, \code{estimate},
\code{std.error}, \code{conf.low}, \code{conf.high}, \code{n_cells}, \code{period}), plus the overall
\code{att}, the \code{se.method}, and metadata.
}
\description{
Decomposes a fitted \code{\link[=trop]{trop()}} ATT into per-period treatment effects indexed by
event time (periods relative to each unit's first treated period) and attaches
pointwise standard errors and confidence intervals. The same resampling used
for the overall SE is reused: each resample's per-cell effects are aggregated
by event time rather than into a single mean, so no extra modelling
assumptions are introduced. With \code{pre_periods = TRUE} the pre-treatment gaps
are returned as placebo / pre-trend points (a flat, near-zero pre-period
profile supports the design).
}
\details{
Because the treated composition varies across bootstrap resamples and each
event time draws on relatively few cells, the per-period intervals are wider
than the overall ATT interval and cover each point individually (they are not
simultaneous bands).
}
\examples{
\donttest{
df  <- sim_panel(N = 16, T = 9, n_treated = 4, t0 = 6, att = 3, seed = 1)
fit <- trop(df, "y", "w", "id", "t",
            control = trop_control(n_cv_cells = 5L, cv_cycles = 1L))
es  <- trop_event_study(fit, se = "bootstrap", pre_periods = TRUE,
                        control = trop_control(n_boot = 20L, seed = 1))
es
}
}
\references{
Athey, S., Imbens, G. W., Qu, Z., & Viviano, D. (2026).
Triply Robust Panel Estimators. \emph{Journal of Applied Econometrics}. \doi{10.1002/jae.70061}.
}
\seealso{
\code{\link[=autoplot.trop_event_study]{autoplot.trop_event_study()}} to plot the result.
}
